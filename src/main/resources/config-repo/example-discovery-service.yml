service:
  port: DISCOVERY_PORT

eureka:
  instance:
    hostname: EUREKA_HOSTNAME
  client:
    register-with-eureka: DISCOVERY_REGISTER_WITH_EUREKA
    fetch-registry: DISCOVERY_FETCH_REGISTRY
    service-url:
      defaultZone: http://${eureka.instance.hostname}:${service.port}/eureka/